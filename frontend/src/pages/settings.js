import React from "react";

export default function Settings() {
    return (
        <div>
        <h1>Settings</h1>
        <p>Settings Settings Settings Settings Settings Settings Settings Settings </p>
        </div>
    );
    }